# 🚨 policies 테이블 삭제 문제 해결 및 데이터 복구 계획

## 📋 문제 상황 요약
- **발생일시**: 데이터 수집 파이프라인 실행 중
- **문제**: policies 테이블이 **통째로 사라짐**
- **영향도**: Critical - 전체 정책 데이터 손실

## 🔍 원인 분석 (완료)

### 1차 원인: find_expired_policies 로직 오류
```python
# 🚨 문제가 있던 코드:
grace_date = datetime.now() - timedelta(days=30)  # 30일 전
query = "WHERE aply_end_ymd < %s"  # 30일 전보다 이전에 만료된 모든 정책!
```
**→ 대부분의 정책이 "만료된 정책"으로 잘못 분류되어 대량 삭제됨**

### 2차 원인: 테이블명 혼용 문제
- **Lambda4**: `youth_policies` 테이블을 기본값으로 사용
- **실제 스키마**: `policies` 테이블만 존재
- **결과**: 일관성 없는 테이블 참조

## ✅ 해결 완료 사항 (2025-01-03 15:37)

### 1. 코드 수정 완료
- **find_expired_policies 로직 수정**: 현재 날짜 기준 + 유예기간 체크
- **대량 삭제 방지**: 최대 500개 제한, 100개씩 처리
- **테이블명 통일**: 모든 Lambda에서 `policies` 테이블 사용
- **안전장치 추가**: 테이블 존재 확인, 상세 로깅

### 2. AWS Lambda 재배포 완료
- **Lambda4 (RDS 저장)**: ✅ 업데이트 완료
  - POLICIES_TABLE = "policies" 
  - CLASSIFICATIONS_TABLE = "policy_classifications"
- **Lambda5 (DB 유지보수)**: ✅ 업데이트 완료
  - 안전장치 적용된 새 코드

### 3. PR 생성 완료
- **PR #80**: [🚨 CRITICAL: policies 테이블 대량 삭제 버그 수정](https://github.com/SKNETWORKS-FAMILY-AICAMP/SKN10-FINAL-5Team/pull/80)
- **상태**: develop 브랜치로 merge 대기

## 🔧 데이터 복구 계획

### 즉시 실행 (Priority 1)
1. **데이터베이스 상태 확인**
   ```sql
   -- 테이블 존재 확인
   SELECT table_name FROM information_schema.tables 
   WHERE table_schema = 'public' AND table_name = 'policies';
   
   -- 데이터 개수 확인
   SELECT COUNT(*) FROM policies;
   ```

2. **백업 데이터 확인**
   - RDS 자동 백업 스냅샷 확인
   - S3 백업 데이터 확인
   - CloudWatch Logs에서 마지막 정상 작동 시점 확인

### 복구 방법 (Priority 2)

#### 방법 1: RDS 스냅샷 복구 (권장)
```bash
# 최근 스냅샷 목록 확인
aws rds describe-db-snapshots --db-instance-identifier youth-policy-api-postgres

# 특정 시점으로 복구 (Point-in-time recovery)
aws rds restore-db-instance-to-point-in-time \
    --source-db-instance-identifier youth-policy-api-postgres \
    --target-db-instance-identifier youth-policy-api-postgres-recovered \
    --restore-time 2025-01-03T06:00:00.000Z
```

#### 방법 2: S3 백업 데이터 재로드
```python
# Lambda1 재실행으로 원시 데이터 다시 수집
# Lambda2, Lambda3, Lambda4 순차 실행으로 파이프라인 재구성
```

#### 방법 3: 수동 데이터 복구
```sql
-- policies 테이블 재생성
CREATE TABLE IF NOT EXISTS policies (
    plcy_no VARCHAR(50) PRIMARY KEY,
    plcy_nm TEXT NOT NULL,
    -- ... 나머지 컬럼들
);

-- 백업 데이터에서 INSERT
```

### 검증 및 모니터링 (Priority 3)

#### 복구 후 검증
1. **데이터 무결성 확인**
   ```sql
   SELECT COUNT(*) as total_policies FROM policies;
   SELECT COUNT(DISTINCT plcy_no) as unique_policies FROM policies;
   ```

2. **파이프라인 테스트**
   - Lambda 함수들 개별 테스트
   - 전체 파이프라인 End-to-End 테스트

3. **모니터링 강화**
   - CloudWatch 알람 설정 (테이블 row count 모니터링)
   - 일일 데이터 백업 자동화

## 🛡️ 재발 방지 조치

### 1. 코드 레벨
- ✅ 대량 삭제 방지 안전장치 적용
- ✅ 테이블명 일관성 확보
- ✅ 상세 로깅 및 예외 처리 강화

### 2. 인프라 레벨
- **정기 백업 자동화**: 매일 RDS 스냅샷
- **모니터링 알람**: 테이블 데이터 급감 시 알림
- **롤백 계획**: 빠른 복구를 위한 자동화 스크립트

### 3. 프로세스 레벨  
- **코드 리뷰 강화**: DB 관련 코드 필수 리뷰
- **스테이징 환경**: 프로덕션 배포 전 테스트
- **점진적 배포**: Blue-Green 배포 전략

## 📞 비상 연락망
- **개발팀 리드**: Jenny-Jo
- **인프라 담당**: [담당자명]
- **비상 시간**: 즉시 대응 체계

## 📅 복구 타임라인
- **즉시**: 데이터베이스 상태 확인
- **30분 이내**: 복구 방법 결정
- **1시간 이내**: 복구 작업 시작
- **2시간 이내**: 복구 완료 및 검증
- **4시간 이내**: 전체 파이프라인 정상화

---
**작성일**: 2025-01-03 15:40  
**작성자**: AI Assistant  
**최종 업데이트**: 2025-01-03 15:40 